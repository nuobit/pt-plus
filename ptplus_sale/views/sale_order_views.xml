<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Sales order form  -->
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">view.order.form.pt</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <!-- Quantity already sent in issued Proforma Invoices -->
            <xpath
                expr="//field[@name='order_line']/list/field[@name='qty_invoiced']"
                position="after"
            >
                <field
                    name="l10n_pt_proforma_qty_sent"
                    optional="hide"
                    groups="sale.group_proforma_sales"
                />
            </xpath>
            <!-- Smart button showing the Proforma Invoices created from this order -->
            <xpath
                expr="//div[@name='button_box']/button[@name='action_view_invoice']"
                position="after"
            >
                <button
                    name="action_view_l10n_pt_proformas"
                    id="action_view_l10n_pt_proformas"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-file-text-o"
                    invisible="l10n_pt_proforma_count == 0"
                >
                    <field
                        name="l10n_pt_proforma_count"
                        widget="statinfo"
                        string="Proforma Invoices"
                    />
                </button>
                <button
                    name="action_view_l10n_pt_proforma_origin"
                    id="action_view_l10n_pt_proforma_origin"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-arrow-left"
                    invisible="not l10n_pt_proforma_origin_id"
                >
                    <div class="o_stat_info">
                        <span class="o_stat_text">Original Order</span>
                    </div>
                </button>
            </xpath>
            <!-- Disable Pro-forma stuff -->
            <xpath
                expr="//button[@name='action_quotation_send'][@groups='sale.group_proforma_sales'][1]"
                position="attributes"
            >
                <attribute
                    name="invisible"
                >state != 'draft' or invoice_count > 1 or source_billing</attribute>
            </xpath>
            <xpath
                expr="//button[@name='action_quotation_send'][@groups='sale.group_proforma_sales'][2]"
                position="attributes"
            >
                <attribute
                    name="invisible"
                >state == 'draft' or invoice_count >= 1 or source_billing</attribute>
            </xpath>
            <!-- Add a issue button for quotations -->
            <xpath expr="//button[@id='action_confirm']" position="after">
                <button
                    name="action_quotation_sent"
                    id="action_quotation_sent"
                    string="Issue Doc."
                    type="object"
                    invisible="state != 'draft' or not fiscal_document_type_id"
                    help="Lock and issue the document so that it can be printed or sent to the partner. "
                />
                <button
                    name="action_create_proforma_invoice"
                    id="action_create_proforma_invoice"
                    string="Create PRÓ-FORMA invoice"
                    type="object"
                    invisible="state in ('draft', 'cancel') or fiscal_document_type_code == 'PF'"
                    help="Duplicate this document as a Proforma Invoice"
                    groups="sale.group_proforma_sales"
                />
                <button
                    name="action_quotation_send"
                    id="action_send_proforma_invoice"
                    string="Send PRO-FORMA Invoice"
                    type="object"
                    context="{'validate_analytic': True}"
                    invisible="fiscal_document_type_code != 'PF' or state in ('draft', 'cancel')"
                    help="Send this document to the customer as a Proforma Invoice"
                    groups="sale.group_proforma_sales"
                />
            </xpath>
            <!-- Confirm cancellation -->
            <xpath expr="//button[@name='action_cancel']" position="attributes">
                <attribute
                    name="confirm"
                >Are you sure you want to cancel the document?</attribute>
            </xpath>
            <!-- Prohibit second confirmation for same quotation -->
            <xpath expr="//button[@name='action_confirm'][1]" position="attributes">
                <attribute
                    name="invisible"
                >state != 'sent' or l10n_pt_order_id or fiscal_document_type_code in ['FC', 'CC', 'PF']</attribute>
                <attribute
                    name="context"
                >{'validate_analytic': True, 'bypass_field_protection': True}</attribute>
            </xpath>
            <xpath expr="//button[@name='action_confirm'][2]" position="attributes">
                <attribute
                    name="invisible"
                >state != 'draft' or l10n_pt_order_id or fiscal_document_type_code in ['FC', 'CC', 'PF']</attribute>
                <attribute
                    name="context"
                >{'validate_analytic': True, 'bypass_field_protection': True}</attribute>
            </xpath>
            <xpath expr="//field[@name='state']" position="before">
                <field name="fiscal_document_type_code" invisible="1" />
            </xpath>

            <!-- Quotations/Orders cross references-->
            <field name="origin" position="after">
                <field name="l10n_pt_order_id" invisible="not l10n_pt_order_id" />
                <field
                    name="l10n_pt_quotation_id"
                    invisible="not l10n_pt_quotation_id"
                />
                <field
                    name="l10n_pt_date_issued"
                    readonly="state != 'draft'"
                    options="{'datepicker': {'warn_future': true}}"
                />
            </field>

            <!-- Source Billing and Fiscal Document Type-->
            <field name="date_order" position="after">
                <field name="source_billing" invisible="1" />
                <field
                    name="fiscal_document_type_id"
                    string="Document Type"
                    options="{'no_create_edit': True}"
                    domain="[('source_billing', '=', source_billing),
                            ('company_id', '=', company_id),
                            ('section', '=', 'work'),
                            ('type', '!=', 'NE')
                            ]"
                    readonly="state != 'draft'"
                />
            </field>
        </field>
    </record>

    <!-- Insert is_order into context and adjust domain -->
    <!--
    <record id="sale.action_quotations" model="ir.actions.act_window">
        <field name="context">{'hide_sale': True, 'is_order': False}</field>
        <field name="domain">[('is_order', '=', False)]</field>
    </record>
    <record id="sale.action_quotations_with_onboarding" model="ir.actions.act_window">
        <field name="context">{'search_default_my_quotation': 1, 'is_order': False}</field>
        <field name="domain">[('is_order', '=', False)]</field>
    </record>
    <record id="sale.action_orders" model="ir.actions.act_window">
        <field name="context">{'is_order': True}</field>
        <field name="domain">[('is_order', '=', True)]</field>
    </record>
    -->

    <record id="view_order_tree_saft" model="ir.ui.view">
        <field name="name">sale.order.list.saft</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field
                    name="l10n_pt_saft_element_status"
                    widget="badge"
                    optional="hide"
                    decoration-success="l10n_pt_saft_element_status == 'done'"
                    decoration-warning="l10n_pt_saft_element_status == 'needed'"
                    decoration-danger="l10n_pt_saft_element_status == 'error'"
                    decoration-info="l10n_pt_saft_element_status == 'manual'"
                    decoration-primary="l10n_pt_saft_element_status == 'blocked'"
                />
            </field>
        </field>
    </record>

    <record id="action_l10n_pt_toggle_saft_element_sale" model="ir.actions.server">
        <field name="name">(Un)block SAF-T element</field>
        <field name="model_id" ref="sale.model_sale_order" />
        <field name="binding_model_id" ref="sale.model_sale_order" />
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">records.action_l10n_pt_toggle_saft_element_block()</field>
    </record>

    <record id="action_l10n_pt_recompute_saft_element_sale" model="ir.actions.server">
        <field name="name">Recompute SAF-T element</field>
        <field name="model_id" ref="sale.model_sale_order" />
        <field name="binding_model_id" ref="sale.model_sale_order" />
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">records.action_l10n_pt_recompute_saft_element()</field>
    </record>
</odoo>
