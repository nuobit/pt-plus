<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Sales invoice form -->
    <record id="view_move_form_pt_nc" model="ir.ui.view">
        <field name="name">account.move.form.pt.nc</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <field name="auto_post" position="after">
                <field
                    name="l10n_pt_rappel_credit_note"
                    invisible="move_type not in ('out_refund', 'in_refund')"
                />
                <field
                    name="l10n_pt_origin_move_date"
                    invisible="not correction or state == 'draft'"
                    readonly="reversed_entry_id"
                />
            </field>
        </field>
    </record>

    <!-- INV/CRED link -->
    <record id="view_move_form_pt_invcred_link" model="ir.ui.view">
        <field name="name">account.move.form.pt.invcred.link</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <notebook position="inside">
                <page
                    name="refunds"
                    string="Refunds"
                    invisible="move_type not in ('in_invoice', 'out_invoice')"
                >
                    <field
                        name="refund_invoice_ids"
                        nolabel="1"
                        context="{'form_view_ref': 'ptplus_account_credit_note.view_refund_form_pt_invcred_link'}"
                    >
                        <list
                            decoration-info="state == 'draft'"
                            decoration-muted="state == 'cancel'"
                        >
                            <field name="partner_id" column_invisible="1" />
                            <field name="move_type" column_invisible="1" />
                            <field name="invoice_date" />
                            <field name="name" />
                            <field name="invoice_origin" />
                            <field name="ref" />
                            <field name="amount_total" string="Total" />
                            <field name="state" string="Status" />
                        </list>
                    </field>
                </page>
            </notebook>
            <field name="ref" position="after">
                <field name="reversed_entry_id" invisible="1" />
                <field name="move_type" invisible="1" />
                <field
                    name="reversed_entry_id"
                    string="Invoice reference"
                    readonly="1"
                    invisible="not reversed_entry_id or move_type not in ('in_refund', 'out_refund')"
                />
            </field>
        </field>
    </record>
    <record id="view_refund_form_pt_invcred_link" model="ir.ui.view">
        <field name="name">account.refund.form.pt.invcred.link</field>
        <field name="model">account.move</field>
        <field name="priority">999</field>
        <field name="arch" type="xml">
            <form string="Refund Invoices">
                <group>
                    <field name="company_id" invisible="1" />
                    <field name="move_type" invisible="1" />
                    <field name="partner_id" />
                    <field name="invoice_date" />
                    <field name="name" />
                    <field name="invoice_origin" />
                    <field name="ref" />
                    <field name="amount_total" string="Total" />
                    <field name="state" string="Status" />
                </group>
                <field name="invoice_line_ids" nolabel="1">
                    <list>
                        <field name="product_id" />
                        <field name="name" />
                        <field name="account_id" />
                        <field name="quantity" />
                        <field name="price_unit" />
                        <field name="discount" optional="hide" />
                        <field name="tax_ids" widget="many2many_tags" />
                        <field name="price_subtotal" />
                    </list>
                </field>
            </form>
        </field>
    </record>
    <!-- INV/CRED link -->
</odoo>
